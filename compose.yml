 version: "3.9"

 services:
   app:
     build:
       dockerfile: ./Dockerfile
     command:
       - gunicorn
       - wsgi:application
       - --bind
       - "0.0.0.0:8000"
     ports:
       - "8000:8000"
     restart: always
     volumes:
       - ./database:/app/database